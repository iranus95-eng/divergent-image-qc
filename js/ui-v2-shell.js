(function(){'use strict';const stack=['home'];let current='home';function buttons(){return Array.from(document.querySelectorAll('[data-page]'))}function show(page,push){const target=document.querySelector('.workspace[data-workspace="'+page+'"]');if(!target)return;document.querySelectorAll('.workspace').forEach(x=>x.classList.remove('active'));target.classList.add('active');buttons().forEach(b=>b.classList.toggle('active',b.dataset.page===page));document.getElementById('pageTitle').textContent=target.dataset.title||'Divergent Corporation';if(push!==false&&page!==current){stack.push(page);if(stack.length>30)stack.splice(1,1)}current=page;window.scrollTo({top:0,behavior:'auto'})}function back(){if(stack.length>1){stack.pop();show(stack[stack.length-1],false)}else show('home',false)}function home(){stack.length=1;stack[0]='home';show('home',false)}function bind(){buttons().forEach(b=>b.addEventListener('click',()=>show(b.dataset.page,true)));document.querySelectorAll('[data-open]').forEach(c=>c.addEventListener('click',()=>show(c.dataset.open,true)));document.getElementById('btnBack').addEventListener('click',back);document.getElementById('btnHome').addEventListener('click',home);show('home',false)}async function refreshAuth(){const box=document.getElementById('authStatus');try{if(!window.DivergentAuth){box.textContent='โหมดทดสอบ UI V2';return}const s=await window.DivergentAuth.refresh();if(s&&s.confirmed&&s.user){box.textContent=(s.user.display_name||s.user.name||s.user.username||'ผู้ใช้งาน')+' · เชื่อมสิทธิ์แล้ว'}else box.textContent='ยังไม่ได้เข้าสู่ระบบ'}catch(_){box.textContent='ยังไม่ได้เข้าสู่ระบบ'}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{bind();refreshAuth()},{once:true});else{bind();refreshAuth()}})();