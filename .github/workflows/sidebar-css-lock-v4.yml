name: Archived - Sidebar CSS lock V4
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived one-time hotfix. Make future changes through a pull request."
