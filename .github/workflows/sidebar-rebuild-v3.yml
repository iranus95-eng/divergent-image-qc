name: Archived - Sidebar permission rebuild V3
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived one-time migration. Make future changes through a pull request."
